# ── Deployer wallet private key ───────────────────────────────────────────────
# Export from MetaMask → Account Details → Export Private Key
# ⚠️  Never commit the real .env file — add it to .gitignore

WALLET_PRIVATE_KEY=


# ── Base Sepolia RPC ──────────────────────────────────────────────────────────
# Free public endpoint (no key needed):
NETWORK_URL=
